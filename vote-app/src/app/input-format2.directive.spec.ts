import { InputFormat2Directive } from './input-format2.directive';

describe('InputFormat2Directive', () => {
  it('should create an instance', () => {
    const directive = new InputFormat2Directive();
    expect(directive).toBeTruthy();
  });
});
