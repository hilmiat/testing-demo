import { KecualiDirective } from './kecuali.directive';

describe('KecualiDirective', () => {
  it('should create an instance', () => {
    const directive = new KecualiDirective();
    expect(directive).toBeTruthy();
  });
});
